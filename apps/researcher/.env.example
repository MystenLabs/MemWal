# OpenRouter API key — chat, LLM chunking, and embeddings
# Get it from https://openrouter.ai/keys
OPENROUTER_API_KEY=

# PostgreSQL (with pgvector extension)
# Local: postgresql://user:pass@localhost:5432/researcher
POSTGRES_URL=

# MemWal V2 SDK — Ed25519 delegate key for memory layer
MEMWAL_KEY=
MEMWAL_SERVER_URL=http://localhost:3001

# Auth secret for NextAuth sessions
# Generate: openssl rand -base64 32
AUTH_SECRET=
