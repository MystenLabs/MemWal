---
description: Disconnect this client from Walrus Memory.
argument-hint: [confirm logout]
---

Call `memwal_logout` to remove local Walrus Memory credentials for this client.

Before calling it, confirm the user really wants to disconnect, because they will need to run `/memwal:setup` or `memwal_login` again before using memory tools.

User note:

```text
$ARGUMENTS
```

