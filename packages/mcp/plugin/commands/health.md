---
description: Check whether Walrus Memory is connected and healthy.
argument-hint: [optional note]
---

Call `memwal_health` and summarize the result.

If credentials are missing, tell the user to run `/memwal:setup` or call `memwal_login`.

Optional context:

```text
$ARGUMENTS
```

