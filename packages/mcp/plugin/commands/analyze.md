---
description: Extract durable facts from text and save them to Walrus Memory.
argument-hint: <text to analyze>
---

Use `memwal_analyze` to extract durable facts from the text below and save them as separate Walrus Memory entries.

Do not save secrets, private keys, access tokens, passwords, or one-time codes.

Text:

```text
$ARGUMENTS
```

