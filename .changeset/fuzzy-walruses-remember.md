---
"@mysten-incubation/memwal-mcp": minor
---

Add `auto_save_user_facts_to_memory` as the primary single-fact memory tool, retain `memwal_remember` as a deprecated API 1.x alias, publish the proactive Walrus Memory MCP prompt, and update automatic-memory guidance.
