<!--
Walrus Memory ships external release notes on https://docs.wal.app/changelog
alongside Walrus / Walrus Sites. Structure user facing PRs the same way so
tracking stays uniform. Internal only PRs can leave the Summary section as N/A.
-->

## Summary (external facing)

<!--
One short paragraph of user or developer impact. Write it so release tracking
can paste it into docs.wal.app/changelog with light edits.

Good (Walrus Sites style):
  The portal now talks to Sui over gRPC instead of JSON-RPC, in preparation
  for the JSON-RPC deprecation.

Bad:
  refactor middleware and fix stuff
-->

## Changes

<!--
Bullets with an area prefix, same pattern as Walrus Sites release notes
(`portal:`, `site-builder:` there → use MemWal areas here).

Areas: `mcp:` `sdk:` `python-sdk:` `server:` `plugin:` `docs:` `ci:` `app:`
-->

-
-

## Test plan

- [ ]
- [ ]

## Changelog

<!--
Pick one. If this should show up on docs.wal.app/changelog, say which package
and whether the Summary/Changes above are ready to paste.
-->

- [ ] Not user facing (skip public changelog)
- [ ] User facing for: `mcp` / `sdk` / `python-sdk` / `server` / `plugin` (circle one)
- [ ] Summary + Changes above are changelog ready
